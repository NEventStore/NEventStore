- Add multi-thread safe tracking of heads for streams to facilitate optimistic concurrency detection with document databases
  when the application code is using an identity map.

- Create fluent configuration code that doesn't require an IoC container.
- Add additional samples and quickstart examples
- Create adapters for the following storage engines (in no particular order)

	- Relational Databases -
	[DONE] Microsoft SQL Server (2000 or later)
	[DONE] Microsoft SQL Azure
	[DONE] MySQL (4 or later) (MyISAM, InnoDB, NDB/Cluter, Drizzle, MariaDB, BerkleyDB, etc.)
	[DONE] Amazon RDS (MySQL)
	[DONE] SQLite (3 or later)
	[DONE] PostgreSQL (8 or later)
	Microsoft Access 2000 (or later)
	Microsoft SQL Compact Edition (4.0 or later)
	Oracle (8 or later)
	Firebird
	VistaDB

	(TODO: Create SqlPersistenceFactory such that the persistence adapter can be entirely self contained.)
	
	- Document Databases -
	CouchDB
	MongoDB
	RavenDB

	- File System
	Managed storage using .NET APIs.

	- Dynamo Clones -
	Cassandra
	Riak
	Voldemort
	Dynomite

	- Other NoSQL -
	Azure Table Storage
	Amazon SimpleDB
	Amazon S3
	Tokyo Cabinet
	HBase
	Redis
	Memcached (and variants)
	Keyspace
	OrientDB / OrientKV
	VoltDB
	BerkleyDB
	Hibari
	HampsterDB